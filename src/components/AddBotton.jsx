import styled from "styled-components";

const StyledButton = styled.button`

`;

function AddButton () {
    return (
        <StyledButton>
            Make a Post
        </StyledButton>
    )
}

export default AddButton;